# TODO(Barry): Move this to midround
- type: entity
  parent: BaseGameRule
  id: BlobAntagRule
  components:
  - type: BlobAntagRule
  - type: GameRule
    minPlayers: 15
    minTotalPlayers: 40
    delay:
      min: 60
      max: 120
  - type: AntagObjectives
    objectives:
    - BlobObjective
  - type: AntagSelection
    definitions:
    - prefRoles: [ BlobAntag ]
      min: 2
      max: 6
      playerRatio: 10
      mindRoles:
      - MindRoleBlobAntag
  # TODO(Barry): This should probably have a spawn component on it, either space or random.
